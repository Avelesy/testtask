apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus  # Укажите тип вашего data source
    url: http://prometheus:9090  # URL вашего data source
    access: proxy
    isDefault: true
